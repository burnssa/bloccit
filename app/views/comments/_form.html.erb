<%= simple_form_for [@post.comments.build]  do |f| %>
    <%= f.label "Submit a comment" %>
    <%= f.text_area :body, rows: 1, class: 'form-control', placeholder: "Enter comment" %>
  <div class="form-group">
    <%= f.submit "Post Comment", class: 'btn btn-success' %>
  </div>
<% end %>